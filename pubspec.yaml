name: reduced_statesrebuilder
version: 0.3.0
description: Implementation of the state management API 'reduced' on top of the state management framework 'StatesRebuilder'.
homepage: https://github.com/partmaster/reduced_statesrebuilder
repository: https://github.com/partmaster/reduced_statesrebuilder.git
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
#  reduced: 0.3.0
  reduced: 
    git:
      url: https://github.com/partmaster/reduced.git
      ref: v0.3.0
  states_rebuilder: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
